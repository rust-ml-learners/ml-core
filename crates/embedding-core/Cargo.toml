[package]
name = "embedding-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "embedding-core - Educational ML primitive"

[dependencies]

[lib]
name = "embedding_core"
path = "src/lib.rs"
